npm install -g typescript
npm install @types/aws-lambda   
-you can see console.logs in cloudwatch console
-package.json and tsconfig modifications are important
-coded in ES module, not commonjs, used .js extension as I declared type as module in json
